name: Playwright Tests

on:
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Set up environment variables for Playwright
        run: |
          echo 'CORRECT_LOGIN="Dafoe"' >> .env
          echo 'CORRECT_LOGIN2="Will"' >> .env
          echo 'CORRECT_LOGIN3="Daf"' >> .env
          echo 'CORRECT_LOGIN4="Willem Dafoe"' >> .env
          echo 'INCORRECT_LOGIN="Follet Verd"' >> .env
          echo 'INCORRECT_LOGIN2="Green Dwarf"' >> .env
          echo 'ROOM="0440"' >> .env
          echo 'ROOM2="440"' >> .env
          echo 'WRONG_ROOM="00440"' >> .env
          echo 'WRONG_ROOM2="9999"' >> .env
          echo 'DEMOHUB="Hotel Demo Hub"' >> .env
          echo 'BASE_URL="https://dev.hub-buildings."' >> .env

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Set up environment variables for Playwright
        run: |
          echo 'CORRECT_LOGIN="Dafoe"' >> .env
          echo 'CORRECT_LOGIN2="Will"' >> .env
          echo 'CORRECT_LOGIN3="Daf"' >> .env
          echo 'CORRECT_LOGIN4="Willem Dafoe"' >> .env
          echo 'INCORRECT_LOGIN="Follet Verd"' >> .env
          echo 'INCORRECT_LOGIN2="Green Dwarf"' >> .env
          echo 'ROOM="0440"' >> .env
          echo 'ROOM2="440"' >> .env
          echo 'WRONG_ROOM="00440"' >> .env
          echo 'WRONG_ROOM2="9999"' >> .env
          echo 'DEMOHUB="Hotel Demo Hub"' >> .env
          echo 'BASE_URL="https://dev.hub-buildings."' >> .env

      - name: Run Playwright tests
        run: npx playwright test

      - name: Show Playwright report files
        if: always()
        run: |
          echo "Report folder listing:"
          ls -la playwright-report || true
          echo "Report files (up to depth 3):"
          find playwright-report -maxdepth 3 -type f -print || true

      - name: Zip Playwright report (if present)
        if: always()
        run: |
          if [ -d "playwright-report" ]; then
            echo "Zipping playwright-report to playwright-report.zip"
            rm -f playwright-report.zip || true
            zip -r playwright-report.zip playwright-report || true
            echo "Zip created:"
            ls -la playwright-report.zip || true
          else
            echo "No playwright-report folder found; skipping zip."
          fi

      - name: Upload Playwright report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          # prefer the zipped report for a single-file artifact, but also include the folder as fallback
          path: |
            playwright-report.zip
            playwright-report
            playwright-report/**

name: Playwright Tests

on:
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Set up environment variables for Playwright
        run: |
          echo 'CORRECT_LOGIN="Dafoe"' >> .env
          echo 'CORRECT_LOGIN2="Will"' >> .env
          echo 'CORRECT_LOGIN3="Daf"' >> .env
          echo 'CORRECT_LOGIN4="Willem Dafoe"' >> .env
          echo 'INCORRECT_LOGIN="Follet Verd"' >> .env
          echo 'INCORRECT_LOGIN2="Green Dwarf"' >> .env
          echo 'ROOM="0440"' >> .env
          echo 'ROOM2="440"' >> .env
          echo 'WRONG_ROOM="00440"' >> .env
          echo 'WRONG_ROOM2="9999"' >> .env
          echo 'DEMOHUB="Hotel Demo Hub"' >> .env
          echo 'BASE_URL="https://dev.hub-buildings."' >> .env

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Install zip
        run: sudo apt-get update && sudo apt-get install -y zip

      - name: Set up environment variables for Playwright
        run: |
          echo 'CORRECT_LOGIN="Dafoe"' >> .env
          echo 'CORRECT_LOGIN2="Will"' >> .env
          echo 'CORRECT_LOGIN3="Daf"' >> .env
          echo 'CORRECT_LOGIN4="Willem Dafoe"' >> .env
          echo 'INCORRECT_LOGIN="Follet Verd"' >> .env
          echo 'INCORRECT_LOGIN2="Green Dwarf"' >> .env
          echo 'ROOM="0440"' >> .env
          echo 'ROOM2="440"' >> .env
          echo 'WRONG_ROOM="00440"' >> .env
          echo 'WRONG_ROOM2="9999"' >> .env
          echo 'DEMOHUB="Hotel Demo Hub"' >> .env
          echo 'BASE_URL="https://dev.hub-buildings."' >> .env

      - name: Run Playwright tests
        run: npx playwright test --reporter=list,html --workers=2

      - name: Show Playwright report files
        if: always()
        run: |
          echo "Report folder listing:"
          ls -la playwright-report || true
          echo "Report files (up to depth 3):"
          find playwright-report -maxdepth 3 -type f -print || true

      - name: Compress test reports
        if: always()
        run: |
          mkdir -p tmp_upload
          # Zip the Playwright HTML report but exclude very large transient folders
          zip -r tmp_upload/playwright-report.zip playwright-report -x "playwright-report/tmp_artifact/*" "playwright-report/**/data/**" || true
          # Include test-results if present
          if [ -d test-results ]; then zip -r tmp_upload/test-results.zip test-results || true; fi

      - name: Upload combined test artifacts (zipped)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-archive
          path: tmp_upload
          retention-days: 7
          if-no-files-found: warn
          

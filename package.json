{
  "name": "hubos-xga",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:ts": "ts-node main.ts",
    "build": "tsc",
    "install-playwright": "npx playwright install",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:headed:chromium": "npx playwright test --headed --browser=chromium",
    "test:headed:firefox": "npx playwright test --headed --browser=firefox",
    "test:headed:webkit": "npx playwright test --headed --browser=webkit",
    "test:headed:file": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^20.5.1",
    "playwright": "^1.37.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "dotenv": "^16.0.0"
  }
}

# Example .env file for Playwright tests
# Do NOT use real credentials in this file for public repos


## Example .env file for Playwright tests
## Copy this file to `.env` and fill with real values (do NOT commit secrets).

# Primary valid login names (used by tests)
CORRECT_LOGIN=Willem\ Dafoe
CORRECT_LOGIN2=Will
CORRECT_LOGIN3=Daf
CORRECT_LOGIN4=Willem\ Dafoe

# Incorrect / negative test values
INCORRECT_LOGIN="Incorrect Surname"
INCORRECT_LOGIN2="Other Wrong"

# Room numbers (leading zeros allowed where needed)
ROOM=0440
ROOM2=440
WRONG_ROOM=00440
WRONG_ROOM2=9999

# Display / application values
DEMOHUB="Hotel Demo Hub"

# Base URL for the application under test. Replace with your environment URL.
# Example: https://dev.hub-buildings.com or http://localhost:3000
BASE_URL=https://dev.hub-buildings.com

# Notes:
# - Keep this file free of real secrets or long encoded tokens.
# - Use `.env` for local secrets and add `.env` to `.gitignore`.
